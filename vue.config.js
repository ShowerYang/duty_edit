module.exports = { 
  // project deployment base
  publicPath: '',  
  // where to output built files
  outputDir: 'dist',
  // where to put static assets (js/css/img/font/...)
  assetsDir: 'static_duty_edit',
  // filename for index.html (relative to outputDir)
  indexPath: 'index.html',
}
